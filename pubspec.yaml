name: network
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none
environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  dio: ^5.9.0
  logger: ^2.6.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
