name: network
description: A lightweight Dart networking library built on Dio with automatic token refresh, request cancellation, typed responses, and comprehensive error handling.
version: 1.0.0
homepage: https://github.com/tarek360/flutter_networking
repository: https://github.com/tarek360/flutter_networking
topics:
  - networking
  - http
  - dio
  - api
  - rest

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  dio: ^5.9.0
  logger: ^2.6.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
